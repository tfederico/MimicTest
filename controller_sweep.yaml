entity: mrroboto
project: tuner
program: deep_mimic.env.simple_hand_stable_pd
command:
  - ${env}
  - ${interpreter}
  - '-m'
  - ${program}
  - ${args}
method: bayes
metric:
  name: error
  goal: minimize
parameters:
  kp:
    min: 0.0
    max: 1000.0
  kd:
    min: 0.0
    max: 10.0